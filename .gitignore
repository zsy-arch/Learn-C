# 编译产物：本项目的教学代码全是"随手编译随手跑"，可执行文件不应该进 git。
#
# 这里大多数 demo 编译产物直接是"源文件名去掉 .c"（比如
# c-experiments/05_pointer/p01_basics.c -> p01_basics），没有固定后缀、
# 没有扩展名。项目里所有真正的源码/文档都带扩展名（.c/.h/.md/.sh/.log/.s/.i），
# 唯一的例外是 Makefile。所以规则是：先精确忽略已知的编译产物目录/后缀，
# 再把"没有扩展名的文件"整体忽略、最后显式放行 Makefile。

# 调试符号包、对象文件、build 目录（Makefile 生成）、依赖文件
*.dSYM/
*.o
*.d
build/

# 没有扩展名的文件默认当成可执行文件忽略
*
!*.*
!Makefile

# macOS / 编辑器杂项
.DS_Store
*.swp
